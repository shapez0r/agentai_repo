{
  "name": "connection-tester",
  "version": "0.1.0",
  "private": true,
  "homepage": "https://shapez0r.github.io/agentai_repo/connection-tester",
  "dependencies": {
    "cra-template-pwa": "2.0.0",
    "gh-pages": "^6.3.0",
    "leaflet": "^1.9.4",
    "react": "^19.1.0",
    "react-dom": "^19.1.0",
    "react-leaflet": "^5.0.0",
    "react-scripts": "^5.0.1",
    "ws": "^8.16.0",
    "express": "^4.18.2"
  },
  "scripts": {
    "start": "react-scripts start",
    "start:server": "node server.js",
    "prebuild": "bash ./replace-commit-hash.sh",
    "build": "react-scripts build",
    "postbuild": "",
    "predeploy": "npm run build",
    "deploy": "gh-pages -b gh-pages -d build --dest connection-tester",
    "deploy:server": "git subtree push --prefix server origin server",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "cross-env": "^7.0.3"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}
